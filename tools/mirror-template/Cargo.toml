[package]
name = "relay-mirror"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
spacetimedb = "2.2"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
